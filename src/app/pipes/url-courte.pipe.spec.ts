import { UrlCourtePipe } from './url-courte.pipe';

describe('UrlCourtePipe', () => {
  it('create an instance', () => {
    const pipe = new UrlCourtePipe();
    expect(pipe).toBeTruthy();
  });
});
